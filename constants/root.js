const ROOT_MENU = document.getElementById('menu');
const ROOT_HEADER = document.getElementById('header');
const ROOT_CART = document.getElementById('cart');
const ROOT_FOOTER = document.getElementById('footer');
const WINDOW_NOTICE = document.getElementById('window');
const NOTICEBLOCK = document.getElementById('madalNotice');